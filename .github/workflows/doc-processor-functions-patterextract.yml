
name: Build and deploy .NET Azure Function App - ExtractPattern

on:
  push:
    branches: [ main ]
    paths:
      - 'DocumentProcessorApp/DocumentProcessor.Functions.ExtractPattern/**'
      - 'DocumentProcessorApp/DocumentProcessor.Application/**'
      - 'DocumentProcessorApp/DocumentProcessor.Common/**'
      - 'DocumentProcessorApp/DocumentProcessor.Constants/**'
      - 'DocumentProcessorApp/DocumentProcessor.Domain/**'
      - 'DocumentProcessorApp/DocumentProcessor.Infrastructure/**'
      - 'DocumentProcessorApp/DocumentProcessor.Tests/**'
  workflow_dispatch:

env:
  DOTNET_VERSION: '8.0.x'

jobs:
  build-and-deploy:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET ${{ env.DOTNET_VERSION }}
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Publish
        run: >
          dotnet publish
          DocumentProcessorApp/DocumentProcessor.Functions.ExtractPattern/DocumentProcessor.Functions.ExtractPattern.csproj
          --configuration Release
          --output "${{ github.workspace }}\\publish"

      - name: Deploy to Azure Functions (ExtractPattern)
        uses: Azure/functions-action@v1
        with:
          app-name: 'doc-processor-function-app'
          package: "${{ github.workspace }}\\publish"
          publish-profile: ${{ secrets.AZURE_FUNCTIONS_PATTERNEXTRACT_PUBLISH_PROFILE }}





